<%- include("partials/header.ejs") %>

<div class="container mt-5">
    <div class="row justify-content-center">
        <h1>Edit <%= game.title %> Post</h1>
    </div>
    <div class="row">
        <div id="newEditForm">
            <form action="/games/<%= game._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <label>Title</label>
                    <input class="form-control" type="text" name="title" placeholder="Title" value="<%= game.title %>" required>
                </div>
                <div class="form-group">
                    <label>Screenshot</label>
                    <input class="form-control" type="text" name="screenshot" placeholder="Screenshot URL" value="<%= game.screenshot %>" required>
                </div>
                <div class="form-group">
                    <label>Review</label>
                    <textarea class="form-control" name="review" placeholder="Write something here..." required><%= game.review %></textarea>
                </div>
                <div class="form-group">
                    <label>Author</label>
                    <input style="width: 50%;" class="form-control" type="text" name="author" placeholder="Author" value="<%= game.author %>" required>
                </div>
                <button class="btn btn-primary">Submit</button>
                <a href="/games" id="goBackLink">Go Back</a>
            </form>
        </div>
    </div>
</div>


<%- include("partials/footer.ejs") %>